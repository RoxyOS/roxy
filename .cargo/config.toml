[alias]
xtask = "run -p xtask --"
xcheck = "run -p xtask -- check"
xtest = "run -p xtask -- test"
xrun = "run -p xtask -- run"
xagent-debug = "run -p xtask -- debug"
rootfs = "run -p xtask -- rootfs"
