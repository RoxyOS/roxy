diff --git xf86-input-evdev-clean/configure xf86-input-evdev-workdir/configure
index 7b579d7..bc5f815 100755
--- xf86-input-evdev-clean/configure
+++ xf86-input-evdev-workdir/configure
@@ -17795,16 +17795,10 @@ fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$UDEV_PKG_ERRORS" >&5
 
-	as_fn_error $? "Package requirements (libudev) were not met:
-
-$UDEV_PKG_ERRORS
-
-Consider adjusting the PKG_CONFIG_PATH environment variable if you
-installed software in a non-standard prefix.
-
-Alternatively, you may set the environment variables UDEV_CFLAGS
-and UDEV_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details." "$LINENO" 5
+	{ $as_echo "$as_me:${as_lineno-$LINENO}: libudev not found, building without it" >&5
+$as_echo "$as_me: libudev not found, building without it" >&6; }
+	UDEV_CFLAGS=""
+	UDEV_LIBS=""
 elif test $pkg_failed = untried; then
      	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
 $as_echo "no" >&6; }
diff --git xf86-input-evdev-clean/src/emuMB.c xf86-input-evdev-workdir/src/emuMB.c
index b25eac8..a13159e 100644
--- xf86-input-evdev-clean/src/emuMB.c
+++ xf86-input-evdev-workdir/src/emuMB.c
@@ -261,9 +261,7 @@ EvdevMBEmuFilterEvent(InputInfoPtr pInfo, int button, BOOL press)
 }
 
 
-void EvdevMBEmuWakeupHandler(pointer data,
-                             int i,
-                             pointer LastSelectMask)
+void EvdevMBEmuWakeupHandler(WAKEUP_HANDLER_ARGS)
 {
     InputInfoPtr pInfo = (InputInfoPtr)data;
     EvdevPtr     pEvdev = (EvdevPtr)pInfo->private;
@@ -277,9 +275,7 @@ void EvdevMBEmuWakeupHandler(pointer data,
     }
 }
 
-void EvdevMBEmuBlockHandler(pointer data,
-                            struct timeval **waitTime,
-                            pointer LastSelectMask)
+void EvdevMBEmuBlockHandler(BLOCK_HANDLER_ARGS)
 {
     InputInfoPtr    pInfo = (InputInfoPtr) data;
     EvdevPtr        pEvdev= (EvdevPtr) pInfo->private;
diff --git xf86-input-evdev-clean/src/evdev.c xf86-input-evdev-workdir/src/evdev.c
index 7f4f0c3..9818b59 100644
--- xf86-input-evdev-clean/src/evdev.c
+++ xf86-input-evdev-workdir/src/evdev.c
@@ -37,9 +37,10 @@
 #include <X11/keysym.h>
 #include <X11/extensions/XI.h>
 
-#include <linux/version.h>
 #include <sys/stat.h>
+#ifdef HAVE_LIBUDEV
 #include <libudev.h>
+#endif
 #include <unistd.h>
 #include <errno.h>
 #include <fcntl.h>
@@ -237,6 +238,7 @@ EvdevIsDuplicate(InputInfoPtr pInfo)
 static BOOL
 EvdevDeviceIsVirtual(const char* devicenode)
 {
+#ifdef HAVE_LIBUDEV
     struct udev *udev = NULL;
     struct udev_device *device = NULL;
     struct stat st;
@@ -267,6 +269,13 @@ out:
     udev_device_unref(device);
     udev_unref(udev);
     return rc;
+#else
+    /* Roxy OS evdev devices are backed by kernel devfs, never virtual ACPI
+     * (LNXSYSTM) devices.  libudev is not available, so this function always
+     * returns FALSE, which is the correct semantic for our platform. */
+    (void)devicenode;
+    return FALSE;
+#endif
 }
 
 #ifndef HAVE_SMOOTH_SCROLLING
@@ -1421,11 +1430,10 @@ EvdevAddAbsValuatorClass(DeviceIntPtr device, int want_scroll_axes)
         if (axnum == -1)
             continue;
 
-#if LINUX_VERSION_CODE > KERNEL_VERSION(2, 6, 30)
-        /* Kernel provides units/mm, X wants units/m */
+        /* Kernel provides units/mm, X wants units/m.  Roxy OS exposes
+         * resolution in units/mm, so the scaling below is unconditional. */
         if (pEvdev->absinfo[axis].resolution)
             resolution = pEvdev->absinfo[axis].resolution * 1000;
-#endif
 
         xf86InitValuatorAxisStruct(device, axnum,
                                    atoms[axnum],
diff --git xf86-input-evdev-clean/src/evdev.h xf86-input-evdev-workdir/src/evdev.h
index 4742b43..bf3a631 100644
--- xf86-input-evdev-clean/src/evdev.h
+++ xf86-input-evdev-workdir/src/evdev.h
@@ -36,7 +36,6 @@
 #define EVDEV_H
 
 #include <linux/input.h>
-#include <linux/types.h>
 
 #include <xorg-server.h>
 #include <xf86Xinput.h>
@@ -71,6 +70,19 @@
 #define LogMessageVerbSigSafe xf86MsgVerb
 #endif
 
+/*
+ * xorg-server 21.x changed ServerBlockHandlerProcPtr / ServerWakeupHandlerProcPtr
+ * from three arguments to two.  Dispatch on the input ABI version so the block /
+ * wakeup handlers below match the installed server (see xorg ditil/dix.h).
+ */
+#if GET_ABI_MAJOR(ABI_XINPUT_VERSION) >= 24
+#define BLOCK_HANDLER_ARGS     void *data, void *waitTime
+#define WAKEUP_HANDLER_ARGS    void *data, int i
+#else
+#define BLOCK_HANDLER_ARGS     pointer data, struct timeval **waitTime, pointer LastSelectMask
+#define WAKEUP_HANDLER_ARGS    void *data, int i, pointer LastSelectMask
+#endif
+
 #define EVDEV_MAXBUTTONS 32
 #define EVDEV_MAXQUEUE 32
 
@@ -272,8 +284,8 @@ unsigned int EvdevUtilButtonEventToButtonNumber(EvdevPtr pEvdev, int code);
 /* Middle Button emulation */
 int  EvdevMBEmuTimer(InputInfoPtr);
 BOOL EvdevMBEmuFilterEvent(InputInfoPtr, int, BOOL);
-void EvdevMBEmuWakeupHandler(pointer, int, pointer);
-void EvdevMBEmuBlockHandler(pointer, struct timeval**, pointer);
+void EvdevMBEmuWakeupHandler(WAKEUP_HANDLER_ARGS);
+void EvdevMBEmuBlockHandler(BLOCK_HANDLER_ARGS);
 void EvdevMBEmuPreInit(InputInfoPtr);
 void EvdevMBEmuOn(InputInfoPtr);
 void EvdevMBEmuFinalize(InputInfoPtr);
